<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>statistiques_resultats</title>
    <!-- <link rel="shortcut icon" href="{{ url_for('static', filename='favicon.ico') }}"> -->
    <link rel="shortcut icon" href="{{ url_for('static', filename='favicon_flask_WStation.ico') }}">
    <!-- Bootstrap core CSS -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/bootstrap.min.css') }}">
    <!-- fichier de style -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/styles.css') }}">
</head>
<body class="container opacity-25">
    <h1 class="mt-2">résultats d'analyses</h1>
    <!-- <img src="data:image/png;base64,{{ img_data }}" alt="Graphique"> -->
    <!-- <div id="charts">
        <img src="{{ url_for('static', filename='total_malwares.png') }}" alt="Total Malwares">
    </div> -->
    <div>
        <ul>
            {% if usb_datas_list_content.usb_id %}
            <li><span>{{ usb_datas_list_content.usb_datas_list_content['usb_id'] }}</span>
            {% else %}
            <li><a href="{{ url_for('accueil') }}">Accueil</a>
            {% endif %}
        </ul>
    </div>
    <div>
        {% if usb_datas_list_content %}
            <ul>
                {% for key in usb_datas_list_content %}
                    <li>{{ usb_datas_list_content[key] }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
    <div>
        <ul>
            {% if usb_datas_list_content %}
                {% for usb_data in usb_datas_list_content %}
                    <li>{{ usb_data }}</li>
                {% endfor %}
            {% endif %}
        </ul>
    </div>
    <!-- <div>
        <ul>
            {% if usb_datas_list_content %}
                {% for usb_data in usb_datas_list_content %}
                    <li>
                        <strong>USB ID:</strong> {{ usb_data.usb_id }}<br>
                        <strong>Total Malwares:</strong> {{ usb_data.total_malwares }}<br>
                        <strong>Malware Types:</strong>
                        <ul>
                            {% for type, count in usb_data.malware_types.items() %}
                                <li>{{ type }}: {{ count }}</li>
                            {% endfor %}
                        </ul>
                        <strong>Detection Dates:</strong>
                        <ul>
                            {% for date in usb_data.detection_dates %}
                                <li>{{ date }}</li>
                            {% endfor %}
                        </ul>
                        <strong>Infected Files:</strong>
                        <ul>
                            {% for file in usb_data.infected_files %}
                                <li>{{ file.file_name }} ({{ file.malware_type }})</li>
                            {% endfor %}
                        </ul>
                    </li>
                {% endfor %}
            {% endif %}
        </ul>
    </div> -->
    <script>
        // usb_data = {{ usb_datas_list_content }}
        console.log("usb_datas_list_content:", usb_datas_list_content);  // This will log to the browser console
    </script>
    <!-- Bootstrap core JS -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script src={{ url_for('static', filename='js/bootstrap.min.js') }}></script>
</body>
</html>
